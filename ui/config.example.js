const config = {
    apiEndpoint: "https://example.com/", //the root where index.php is located
    duration: "15" //remember to set the value in seconds in config.php, here you only set the value visible in the UI in minutes
};